export default function AdminPage() {
  return (
    <main style={{ padding: 24 }}>
      <h1>Admin OK (smoke test)</h1>
      <p>If you see this, routing is fine. We’ll restore the real page next.</p>
    </main>
  );
}
