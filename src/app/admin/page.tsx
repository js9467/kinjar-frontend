export default function AdminPage() {
  return (
    <main style={{ padding: 24 }}>
      <h1>/admin OK — smoke test</h1>
      <p>If you see this in production, routing works.</p>
    </main>
  );
}
